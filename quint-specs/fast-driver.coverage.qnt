// Generated by Quint Studio from the oracle's test-proven replays — DO NOT EDIT.
// Regenerated on every published oracle run; the git diff of this file IS the
// behavioral diff. One run per covered behavior, with .expect(<observation>)
// where each observation first fires; at the bottom, one NO-SPEC-REPLAY entry
// per test the model did not reproduce — a run deleted in the same diff that
// gains one of those is a model-code divergence, not lost coverage. A test
// whose every replayed step left the spec state unchanged appears nowhere at
// all: it exercised no transition, so it covers nothing here. A run marked
// @truncated is a PREFIX of a longer proven path — it covers the states
// those steps reach and makes no claim past the cut. Deleting
// anything here deletes nothing — the next regeneration puts it back.
// format: quint-coverage v3
module FastDriverCoverage {
  import fast_driver.* from "fast-driver"

  /// @covered_by an_invalid_proposal_never_supplies_a_decision
  run drivernewDriverprocessDriverprocessDriverprocess =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Proposal", 0, "7", -1, false, "a", "Precommit", "Propose"))
      .expect(fresh_proposal_routed_invalid)
      .expect(invalid_proposal_not_retained)
      .then(Driverprocess("Vote", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "c", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "d", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "e", "Precommit", "Propose"))
      .expect(decision_quorum_waited_for_its_proposal)

  /// @covered_by a_repropose_is_resolved_into_a_full_proposal
  /// @replayable false
  run drivernewDriverprocessDriverprocessDriverprocess2 =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Proposal", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .expect(fresh_proposal_routed_valid)
      .expect(fresh_proposal_retained)
      .then(Driverprocess("Vote", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "c", "Precommit", "Propose"))
      // unpinned args: none logged
      .then(Driverset_proposer)
      .then(Driverprocess("NewRound", 1, "v", -1, true, "a", "Precommit", "Propose"))
      .expect(repropose_resolved_to_a_full_proposal)

  /// @covered_by timeouts_route_by_kind
  run drivernewDriverprocessDriverprocessDriverprocess3 =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("TimeoutElapsed", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("TimeoutElapsed", 0, "v", -1, true, "a", "Precommit", "Prevote"))
      .expect(timeout_kind_without_a_step_ignored)

  /// @covered_by a_quorum_that_arrives_before_its_proposal_still_decides
  run drivernewDriverprocessDriverprocessDriverprocess4 =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "c", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "d", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "e", "Precommit", "Propose"))
      .expect(decision_quorum_waited_for_its_proposal)
      .then(Driverprocess("Proposal", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .expect(fresh_proposal_routed_valid)
      .expect(decision_fired_on_the_proposal_edge)
      .expect(fresh_proposal_retained)

  /// @covered_by a_proposer_that_cannot_build_its_reproposal_still_schedules_a_timeout
  /// @replayable false
  run drivernewDriverprocessDriverprocessDriverprocess5 =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "c", "Precommit", "Propose"))
      // unpinned args: none logged
      .then(Driverset_proposer)
      .then(Driverprocess("NewRound", 1, "v", -1, true, "a", "Precommit", "Propose"))
      .expect(repropose_value_missing)

  /// @covered_by a_justified_reproposal_is_accepted
  /// @replayable false
  run drivernewDriverprocessDriverprocessDriverprocess6 =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "c", "Precommit", "Propose"))
      // unpinned args: none logged
      .then(Driverset_proposer)
      .then(Driverprocess("NewRound", 1, "v", -1, true, "a", "Precommit", "Propose"))
      .expect(repropose_value_missing)
      .then(Driverprocess("Proposal", 1, "7", 0, true, "a", "Precommit", "Propose"))
      .expect(reproposal_routed_as_justified)
      .expect(reproposal_not_retained)

  /// @covered_by a_justified_reproposal_is_accepted
  run drivernewDriverprocessDriverprocessDriverprocess7 =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 0, "7", -1, true, "c", "Precommit", "Propose"))
      .then(Driverset_proposer("b"))
      .then(Driverprocess("NewRound", 1, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Proposal", 1, "7", 0, true, "a", "Precommit", "Propose"))
      .expect(reproposal_routed_as_justified)
      .expect(reproposal_not_retained)

  /// @covered_by a_decision_pairs_a_quorum_with_a_proposal_from_an_earlier_round
  run drivernewDriverprocessDriverprocessDriversetProposer =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Proposal", 0, "7", -1, true, "a", "Precommit", "Propose"))
      .expect(fresh_proposal_routed_valid)
      .expect(fresh_proposal_retained)
      .then(Driverset_proposer("c"))
      .then(Driverprocess("NewRound", 1, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 1, "7", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 1, "7", -1, true, "b", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 1, "7", -1, true, "c", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 1, "7", -1, true, "d", "Precommit", "Propose"))
      .then(Driverprocess("Vote", 1, "7", -1, true, "e", "Precommit", "Propose"))
      .expect(decision_fired_on_the_vote_edge)

  /// @covered_by an_unjustified_reproposal_is_not_accepted
  /// @replayable false
  run drivernewDriverprocessDriversetProposerDriverprocess =
    init
      .then(Drivernew("b"))
      .then(Driverprocess("NewRound", 0, "v", -1, true, "a", "Precommit", "Propose"))
      // unpinned args: none logged
      .then(Driverset_proposer)
      .then(Driverprocess("NewRound", 1, "v", -1, true, "a", "Precommit", "Propose"))
      .then(Driverprocess("Proposal", 1, "7", 0, true, "a", "Precommit", "Propose"))
      .expect(reproposal_routed_as_unjustified)
      .expect(reproposal_not_retained)
}
