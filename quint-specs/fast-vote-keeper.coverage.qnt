// Generated by Quint Studio from the oracle's test-proven replays — DO NOT EDIT.
// Regenerated on every published oracle run; the git diff of this file IS the
// behavioral diff. One run per covered behavior, with .expect(<observation>)
// where each observation first fires; at the bottom, one NO-SPEC-REPLAY entry
// per test the model did not reproduce — a run deleted in the same diff that
// gains one of those is a model-code divergence, not lost coverage. A test
// whose every replayed step left the spec state unchanged appears nowhere at
// all: it exercised no transition, so it covers nothing here. A run marked
// @truncated is a PREFIX of a longer proven path — it covers the states
// those steps reach and makes no claim past the cut. Deleting
// anything here deletes nothing — the next regeneration puts it back.
// format: quint-coverage v3
module FastVoteKeeperCoverage {
  import fast_vote_keeper.* from "fast-vote-keeper"

  /// @covered_by a_vote_at_an_undefined_round_is_discarded
  run fastVoteKeepernewFastVoteKeeperapplyVote =
    init
      .then(FastVoteKeepernew([1, 1, 1], 3))
      .then(FastVoteKeeperapply_vote(0, -1, "1", "Precommit", 0))
      .expect(nil_round_discarded)

  /// @covered_by unknown_validator_is_discarded
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVote2 =
    init
      .then(FastVoteKeepernew([1, 1, 1], 3))
      // unpinned args: outputs=0, value="1", voter=-1
      .then(FastVoteKeeperapply_vote)
      .expect(unknown_voter_discarded)

  /// @covered_by a_vote_at_an_undefined_round_is_discarded
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVote3 =
    init
      .then(FastVoteKeepernew([1, 1, 1], 3))
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(vote_tallied_without_output)

  /// @covered_by a_single_vote_can_cross_both_thresholds_at_once
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVote =
    init
      .then(FastVoteKeepernew([4, 5, 1], 10))
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(vote_tallied_without_output)
      .expect(value_at_exact_quorum_boundary)
      // unpinned args: outputs=7, value="1", voter=1
      .then(FastVoteKeeperapply_vote)
      .expect(vote_quorum_emitted)
      .expect(decision_quorum_emitted)
      .expect(both_quorums_from_one_vote)
      .expect(quorum_any_emitted)

  /// @covered_by a_prevote_is_never_tallied
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      .then(FastVoteKeeperapply_vote(0, 0, "7", "Prevote", 0))
      .expect(prevote_discarded)
      .then(FastVoteKeeperapply_vote(1, 0, "7", "Prevote", 0))
      .then(FastVoteKeeperapply_vote(2, 0, "7", "Prevote", 0))
      .then(FastVoteKeeperapply_vote(3, 0, "7", "Prevote", 0))
      .then(FastVoteKeeperapply_vote(4, 0, "7", "Prevote", 0))
      .then(FastVoteKeeperapply_vote(0, 0, "9", "Precommit", 0))
      .expect(vote_tallied_without_output)

  /// @covered_by pruning_drops_rounds_and_keeps_evidence
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote2 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="2", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(equivocation_recorded)
      .then(FastVoteKeeperapply_vote(0, 5, "9", "Precommit", 0))
      .then(FastVoteKeeperapply_vote(1, 5, "9", "Precommit", 0))
      .then(FastVoteKeeperapply_vote(2, 5, "9", "Precommit", 1))
      .expect(vote_quorum_emitted)
      .then(FastVoteKeeperprune_votes(5, 1))
      .expect(round_pruned)

  /// @covered_by exactly_at_a_threshold_is_not_met
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote3 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1], 5))
      .then(FastVoteKeeperapply_vote(0, 0, "3", "Precommit", 0))
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="3", voter=1
      .then(FastVoteKeeperapply_vote)
      .expect(value_at_exact_quorum_boundary)
      // unpinned args: outputs=1, value="3", voter=2
      .then(FastVoteKeeperapply_vote)
      .expect(vote_quorum_emitted)
      // unpinned args: outputs=0, value="3", voter=3
      .then(FastVoteKeeperapply_vote)
      .expect(value_at_exact_decision_boundary)
      .expect(pruned_round_emits_nothing_again)
      // unpinned args: outputs=6, value="3", voter=4
      .then(FastVoteKeeperapply_vote)
      .expect(decision_quorum_emitted)
      .expect(quorum_any_emitted)

  /// @covered_by equivocation_is_recorded_as_evidence_and_never_tallied
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote4 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="1", voter=1
      .then(FastVoteKeeperapply_vote)
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(duplicate_vote_ignored)
      // unpinned args: outputs=0, value="2", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(equivocation_recorded)

  /// @covered_by future_round_votes_never_produce_a_skip
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote5 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="1", voter=1
      .then(FastVoteKeeperapply_vote)
      // unpinned args: outputs=1, value="1", voter=2
      .then(FastVoteKeeperapply_vote)
      .expect(vote_quorum_emitted)

  /// @covered_by both_thresholds_fire_for_one_round_in_order
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote6 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      .then(FastVoteKeeperapply_vote(0, 0, "7", "Precommit", 0))
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="7", voter=1
      .then(FastVoteKeeperapply_vote)
      // unpinned args: outputs=1, value="7", voter=2
      .then(FastVoteKeeperapply_vote)
      .expect(vote_quorum_emitted)
      // unpinned args: outputs=0, value="7", voter=3
      .then(FastVoteKeeperapply_vote)
      .expect(pruned_round_emits_nothing_again)
      // unpinned args: outputs=6, value="7", voter=4
      .then(FastVoteKeeperapply_vote)
      .expect(decision_quorum_emitted)
      .expect(quorum_any_emitted)

  /// @covered_by each_threshold_reports_at_most_once_per_round
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote7 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      .then(FastVoteKeeperapply_vote(0, 0, "7", "Precommit", 0))
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="7", voter=1
      .then(FastVoteKeeperapply_vote)
      // unpinned args: outputs=1, value="7", voter=2
      .then(FastVoteKeeperapply_vote)
      .expect(vote_quorum_emitted)
      // unpinned args: outputs=0, value="7", voter=3
      .then(FastVoteKeeperapply_vote)
      .expect(pruned_round_emits_nothing_again)
      // unpinned args: outputs=6, value="7", voter=4
      .then(FastVoteKeeperapply_vote)
      .expect(decision_quorum_emitted)
      .expect(quorum_any_emitted)
      // unpinned args: outputs=0, value="7", voter=5
      .then(FastVoteKeeperapply_vote)

  /// @covered_by pruning_keeps_the_cross_round_justification
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote8 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      .then(FastVoteKeeperapply_vote(0, 2, "9", "Precommit", 0))
      .expect(vote_tallied_without_output)
      .then(FastVoteKeeperapply_vote(1, 2, "9", "Precommit", 0))
      .then(FastVoteKeeperapply_vote(2, 2, "9", "Precommit", 1))
      .expect(vote_quorum_emitted)
      .then(FastVoteKeeperapply_vote(3, 2, "9", "Precommit", 0))
      .expect(pruned_round_emits_nothing_again)
      .then(FastVoteKeeperapply_vote(4, 2, "9", "Precommit", 6))
      .expect(decision_quorum_emitted)
      .expect(quorum_any_emitted)
      .then(FastVoteKeeperprune_votes(7, 0))
      .expect(round_pruned)

  /// @covered_by a_pruned_round_does_not_report_its_thresholds_twice
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote9 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      .then(FastVoteKeeperapply_vote(0, 1, "4", "Precommit", 0))
      .expect(vote_tallied_without_output)
      .then(FastVoteKeeperapply_vote(1, 1, "4", "Precommit", 0))
      .then(FastVoteKeeperapply_vote(2, 1, "4", "Precommit", 1))
      .expect(vote_quorum_emitted)
      .then(FastVoteKeeperprune_votes(6, 0))
      .expect(round_pruned)
      .then(FastVoteKeeperapply_vote(0, 1, "4", "Precommit", 0))
      .expect(vote_retallied_after_prune)
      .then(FastVoteKeeperapply_vote(1, 1, "4", "Precommit", 0))
      .then(FastVoteKeeperapply_vote(2, 1, "4", "Precommit", 0))
      .expect(pruned_round_emits_nothing_again)

  /// @covered_by a_split_quorum_reaches_only_quorum_any
  /// @covered_by nil_votes_count_for_quorum_any_but_never_for_a_value
  /// @replayable false
  run fastVoteKeepernewFastVoteKeeperapplyVoteFastVoteKeeperapplyVoteFastVoteKeeperapplyVote10 =
    init
      .then(FastVoteKeepernew([1, 1, 1, 1, 1, 1], 6))
      // unpinned args: outputs=0, value="1", voter=0
      .then(FastVoteKeeperapply_vote)
      .expect(vote_tallied_without_output)
      // unpinned args: outputs=0, value="2", voter=1
      .then(FastVoteKeeperapply_vote)
      .then(FastVoteKeeperapply_vote(2, 0, "3", "Precommit", 0))
      // unpinned args: outputs=0, value="1", voter=3
      .then(FastVoteKeeperapply_vote)
      // unpinned args: outputs=4, value="2", voter=4
      .then(FastVoteKeeperapply_vote)
      .expect(quorum_any_emitted)
      .expect(quorum_any_without_any_value_quorum)
}
